name: schoolapp  # Required: Your app's name (must be unique, lowercase, no spaces)
version: 1.0.0+1  # Example version; increment as needed

environment:
  sdk: '>=3.5.0 <4.0.0'  # Dart SDK constraint (as recommended)

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.0
  shared_preferences: ^2.5.3
  fl_chart: ^1.1.1
  flutter_local_notifications: ^19.5.0
  table_calendar: ^3.2.0
  file_picker: ^10.3.3
  share_plus: ^12.0.1
  path_provider: ^2.1.5

# If you have dev_dependencies (e.g., for tests), add them here
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Example; update as needed

# Other sections like flutter: (for assets, fonts) go here if needed
flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/